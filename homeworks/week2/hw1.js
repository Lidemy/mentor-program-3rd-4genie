// eslint-disable-next-line space-before-function-paren
function printStars(n) {
  // 設定變數 starts 為'*'
  const starts = '*';
  for (let i = 0; i < n; i += 1) {
    console.log(starts);
  }
}

printStars(5);

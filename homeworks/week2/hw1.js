/* eslint-disable no-multi-spaces */
// eslint-disable-next-line space-before-function-paren
function printStars(n) {
  const starts = '*';               // 設定變數 starts 為'*'
  for (let i = 0; i < n; i += 1) {
    console.log(starts);
  }
}

printStars(5);
